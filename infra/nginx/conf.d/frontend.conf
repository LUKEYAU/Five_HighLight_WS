server {
  listen 80;
  server_name fivecut.school.edu;

  # 你有 TLS 就在 443 的 server 配同設定；先給 80 版
  location / {
    proxy_pass http://frontend:5173;
    # Vite HMR 也會用到 Upgrade
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}
